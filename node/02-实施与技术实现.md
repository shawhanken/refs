# Cowboy Node PVM 集成：实施与技术实现

**最后更新**: 2026-01-24  
**实施日期**: 2026-01-10 ~ 2026-01-18  
**编译状态**: ✅ 成功通过

---

## 📋 实施总结

### 已完成的工作

#### 阶段1: 解决借用检查器问题 ✅

**问题分析**:
- 原始代码有 7+ 个借用检查器错误
- 根本原因：PVM 的 `HostGuard` 使用 `unsafe { mem::transmute }` 擦除生命周期，导致 Rust 编译器无法正确推断 async 函数中的借用

**解决方案**:
1. **PVM 侧改进**:
   - 添加了 `ExecutionResult<T>` 类型，支持在成功和失败时都返回状态快照
   - 添加了 `execute_tx_with_options_and_callback` API，允许在执行完成后提取状态
   - 将核心逻辑拆分为 `execute_tx_internal`，保持内部错误处理的一致性
   - 导出 `ContinuationMode` 到公共 API

2. **Node 侧改进**:
   - 将 `Rc<RefCell<>>` 替换为 `Arc<Mutex<>>`，支持 Send trait
   - 创建 `StateSnapshot` 结构来避免后续借用
   - 重构为独立的 `execute_actor_handler_impl` 函数，清晰化 async 边界
   - 使用局部作用域内的裸指针来绕过编译器的保守生命周期分析

**技术细节**:
```rust
// 在需要 reborrow 的地方使用局部指针
{
    let pvm_ctx_ptr: *mut PvmExecutionContext<'_, S> = &mut pvm_ctx;
    unsafe { (*pvm_ctx_ptr).commit().await }
        .map_err(|e| ExecutionError::StoreError(Box::new(e)))?
};
```

**安全性说明**:
- ✅ pvm_ctx 在整个函数作用域内有效
- ✅ 指针仅在局部作用域内使用，不跨 await 点
- ✅ 不会创建别名或违反借用规则
- ✅ 已添加详细注释说明为什么需要 unsafe

#### 阶段2: 完善单块原子性 ✅

**白皮书要求**:
- ✅ 单块内执行的原子性（全部提交或全部回滚）
- ✅ 显式消息传递（所有异步操作通过显式消息）
- ✅ 消息去重（exactly-once 语义）

**实现**:
1. **执行顺序**（完全符合白皮书）:
   ```
   1. 执行 actor 代码 (PVM)
   2. 提交存储变更 (commit)  ← 原子性边界
   3. 处理副作用:
      - 发射事件
      - 发送消息
      - 调度定时器
      - 取消定时器
   ```

2. **消息去重机制**:
   ```rust
   // 使用 seen_messages HashSet 实现 exactly-once 语义
   if self.seen_messages.contains(&outgoing_msg_id) {
       continue;  // Skip duplicate
   }
   self.seen_messages.insert(outgoing_msg_id);
   ```

3. **回滚机制**:
   - 执行失败时调用 `pvm_ctx.rollback()`
   - 清空 storage cache 和所有副作用
   - 确保不会有部分状态提交

#### 阶段3: Continuation 支持 ✅

**白皮书要求**:
- ✅ 支持 Checkpoint 模式（运行时快照）
- ✅ Continuation 状态存储在 actor storage: `__continuation:{cid}`
- ✅ 支持 resume 机制

**实现**:
```rust
let continuation_key = if let Some(msg_id) = &ctx.message_id {
    // Use message ID as continuation ID
    Some(format!("__continuation:{}", hex::encode(msg_id.as_ref())))
} else {
    None
};

continuation: continuation_key.as_ref().map(|key| {
    ContinuationOptions {
        mode: ContinuationMode::Checkpoint,
        checkpoint_key: Some(key.as_bytes().to_vec()),
        resume_key: Some(key.as_bytes().to_vec()),
        ..Default::default()
    }
})
```

#### 阶段4: 确定性执行配置 ✅

**白皮书要求**:
- ✅ 固定 Hash Seed：`PYTHONHASHSEED = 0`
- ✅ SoftFloat：所有浮点运算使用软件浮点库
- ✅ ordered_set：替换内置 `set` 为 `ordered_set`
- ✅ CBOR 序列化：所有跨信任边界的数据使用 Canonical CBOR
- ✅ 禁止非确定性操作：禁止 `time`, `random`, `pickle` 等

**实现**:
```rust
options.determinism = Some(DeterminismOptions {
    hash_seed: 0,
    stdlib_whitelist: Some(vec!["math", "json", ...]),
    stdlib_blacklist: Some(vec!["time", "random", "pickle", ...]),
    ..Default::default()
});
```

---

## 🔧 技术问题解决方案

### 1. 借用检查器问题

#### 问题描述
在 `PvmExecutor::execute_handler` 中，存在借用检查器错误：
```
error[E0502]: cannot borrow `ctx.outgoing_messages` as immutable because it is also borrowed as mutable
```

#### 根本原因
`HostGuard` 使用 `PhantomData<&'a mut dyn HostApi>` 标记生命周期，导致编译器认为对 `ctx` 的借用会持续到整个函数结束，即使 `HostGuard` 实际在 `execute_tx_with_options` 返回时就被 drop 了。

#### 最终解决方案：Arc<Mutex<>>

**核心思路**:
将副作用字段改为 `Arc<Mutex<Vec<...>>>`，这样：
1. `PvmExecutionContext` 持有一个 `Arc` 引用
2. 可以在创建 context 时克隆 `Arc` 引用
3. 在 `execute_handler` 返回后，使用克隆的 `Arc` 引用提取副作用
4. 不需要直接访问被借用的 `ctx`

**代码实现**:
```rust
// pvm_host.rs
pub struct PvmExecutionContext<'a, S: StateStore> {
    // 使用 Arc<Mutex<>> 实现并发安全的共享可变性
    pub events: Arc<Mutex<Vec<(String, Vec<u8>)>>>,
    pub outgoing_messages: Arc<Mutex<Vec<(Vec<u8>, Vec<u8>)>>>,
    pub scheduled_timers: Arc<Mutex<Vec<(u64, Vec<u8>, Vec<u8>)>>>,
    pub cancelled_timers: Arc<Mutex<Vec<Vec<u8>>>>,
}

// 在 execute_handler 返回后提取副作用
let side_effects = ExecutionSideEffects {
    outgoing_messages: mem::take(&mut *ctx.outgoing_messages.lock().unwrap()),
    scheduled_timers: mem::take(&mut *ctx.scheduled_timers.lock().unwrap()),
    cancelled_timers: mem::take(&mut *ctx.cancelled_timers.lock().unwrap()),
    events: mem::take(&mut *ctx.events.lock().unwrap()),
};
```

**优势**:
- ✅ 支持 Send + Sync trait，可以在异步环境中使用
- ✅ 并发安全，多个线程可以安全访问
- ✅ 不需要 unsafe 代码
- ✅ 代码清晰易懂

### 2. Stack Overflow 问题

#### 问题现象
```
thread 'tokio-runtime-worker' has overflowed its stack
fatal runtime error: stack overflow, aborting
```

#### 根本原因
**PVM（基于 RustPython）执行 Python 代码需要大量栈空间**

原因细节：
1. Python 解释器的递归调用
2. 对象序列化/反序列化（snapshot）的深度遍历
3. Tokio worker 线程默认栈大小（2 MiB）不足

#### 解决方案：增加线程栈大小

**采用方案**: 环境变量 `RUST_MIN_STACK`

**修改内容**:
```bash
# test2.sh
export RUST_MIN_STACK=16777216  # 16 MiB = 16 * 1024 * 1024 字节
```

**栈大小对比**:
| 环境 | 默认栈大小 | 修改后栈大小 |
|-----|-----------|------------|
| Rust 主线程 | 8 MiB | 16 MiB |
| Tokio worker | 2 MiB | 16 MiB |
| **增加倍数** | - | **8x** |

**内存影响**:
- Worker 线程数：4-8 个
- 栈大小：16 MiB/线程
- **总栈内存**：64-128 MiB（可接受）

**生产环境部署**:
```bash
# systemd 服务配置
[Service]
Environment="RUST_MIN_STACK=16777216"
ExecStart=/path/to/validator --config=...
```

---

## 🏗️ API 实现

### CowboyHost 实现

**结构体**:
```rust
pub struct CowboyHost<'a, S: StateStore> {
    ctx: &'a mut PvmExecutionContext<'a, S>,
}
```

### HostApi 实现

所有必需的方法都已实现：

1. **状态管理**
   - `state_get()` - 从缓存读取
   - `state_set()` - 写入缓存并计费
   - `state_delete()` - 删除并计费

2. **事件**
   - `emit_event()` - 记录事件并计费

3. **Gas 管理**
   - `charge_gas()` - 消耗 cycles
   - `gas_left()` - 返回剩余 cycles

4. **上下文**
   - `context()` - 返回完整的 HostContext（包含 actor_addr, msg_id, nonce）
   - `randomness()` - 基于 block_hash 生成确定性随机数

5. **消息和定时器**
   - `send_message()` - 记录待发送消息
   - `schedule_timer()` - 生成定时器 ID 并记录
   - `cancel_timer()` - 记录待取消的定时器

### PvmExecutionContext

执行上下文，包含：
- Actor 数据和存储缓存
- 区块上下文（height, hash, timestamp）
- Gas 计量器
- 事件、消息、定时器列表
- Message ID 和 nonce

### 存储缓存机制

使用 `ActorStorageCache` 实现同步存储访问：
- 预加载 actor 存储
- 执行期间累积写入
- 提交时批量写入
- 支持回滚

### Gas 计量

使用双 Gas 系统：
- **Cycles**: 用于计算和操作
- **Cells**: 用于存储操作

PVM 的单一 Gas 模型映射到 cycles。

---

## 📊 架构图

```
┌────────────────────────────────────────┐
│         Cowboy Chain Validator         │
│                                         │
│  ┌──────────────────────────────────┐ │
│  │   TransactionExecutor            │ │
│  │   ├─ seen_messages (去重)       │ │
│  │   ├─ dual_gas_meters             │ │
│  │   └─ execute_actor_handler_impl │ │
│  └──────────────────────────────────┘ │
│                 │                       │
│                 ▼                       │
│  ┌──────────────────────────────────┐ │
│  │   PvmExecutor                    │ │
│  │   ├─ ExecutionOptions            │ │
│  │   │  ├─ Continuation: Checkpoint │ │
│  │   │  └─ Determinism: hash_seed=0 │ │
│  │   └─ execute_handler()         │ │
│  └──────────────────────────────────┘ │
│                 │                       │
│                 ▼                       │
│  ┌──────────────────────────────────┐ │
│  │   CowboyHost (impl HostApi)      │ │
│  │   ├─ state_get/set (Cells)       │ │
│  │   ├─ emit_event (Cells)          │ │
│  │   ├─ send_message (Cells)        │ │
│  │   └─ charge_gas (Cycles)         │ │
│  └──────────────────────────────────┘ │
│                 │                       │
│                 ▼                       │
│  ┌──────────────────────────────────┐ │
│  │   PVM Runtime (RustPython)      │ │
│  │   └─ execute_tx_with_options()   │ │
│  └──────────────────────────────────┘ │
└────────────────────────────────────────┘
```

---

## 📝 关键文件

### 实现文件
- `chain/src/pvm_host.rs` - HostApi 实现
- `chain/src/pvm_executor.rs` - PVM 执行器
- `chain/src/execution.rs` - 交易执行引擎

### 测试文件
- `chain/src/pvm_host.rs` - 6 个单元测试
- `chain/src/pvm_executor.rs` - 8 个单元测试

---

## ⚠️ 技术债务

### 当前技术债务
1. **Unsafe 代码** - 局部使用，已详细注释，风险低
2. **临时方案** - Arc<Mutex<>> 替代方案，未来可优化
3. **性能未充分验证** - 需要生产环境验证

### 未来改进方向
1. 研究 commonware_runtime 是否支持栈大小配置
2. 优化 PVM 代码，减少栈使用
3. Profile 执行，确定性能瓶颈
4. 考虑使用 JIT 或其他优化技术

---

## 🔗 相关文档

- **项目概览与路线图** (`01-项目概览与路线图.md`) - 整体规划和评估
- **测试与验证** (`03-测试与验证.md`) - 测试报告和验证结果
- **当前状态与行动项** (`04-当前状态与行动项.md`) - 当前工作重点

---

**文档版本**: 1.0  
**最后更新**: 2026-01-24
