# CIP-6 SDK 实现程度评估报告

**日期**: 2026-03-03  
**评估范围**: `/home/ubuntu/workspvm/Lib/cowpace/node/ + Rust 宿主绑boy_sdk/`定  
**参考规范**: `/home/ubuntu/workspace/refs/cips/cip-6-sdk.md`

---

## 总体评估

| 维度 | 状态 |
|------|------|
| 整体完成度 | ~58% |
| P0 阻塞缺口 | 2 项 |
| P1 对齐缺口 | 2 项 |
| P2 功能缺口 | 1 项 |

---

## 各章节评估

### 第 1 章 — 类型系统 ✅ 完整

`typesddress`、`or.py` 实现了 `Adered_set`、`BlockHeight`、`SoftFloat`，与规范完全对齐。

### 第 2 章 — FSM Continuation ⚠️ 部分完整

**已实现**:
- `_compiler.py` 支持顶层顺序 await 的 FSM 转换
- `continuation.py` 的 `Capture`/`save_cont`/`load_cont`/`delete_cont` 完整
- `@runner.continuation` 装饰器框架完整

**缺口 (P1)**:
- `save_cont()` 调用未传递 `timeout_blocks` 和 `guard_keys` 参数
- `@actor.continuation` 使用 `runner_addr_expr='ACTOR_RUNNER_ADDRESS'`，但该变量在生成代码命名空间中未定义

### 第 3 章 — Runner 接口 ✅ 完整

`runner.llm`/`runner.http`/`runner.mcp` 接口完整，`RUNNER_ADDRESS` 正确引用 `Address.JOB_DISPATCHER`。

### 第 4 章 — Actor 系统 ⚠️ 部分完整

**已实现**:or` 装饰器注入 
- `@act`address`/`storage`
- `ActorRef` 同步调用语法糖
- `_dispatch_message` 消息路由

**缺口 (P0)**:
- `tor.cont@acinuation` 的 `ACTOR_RUNNER_ADDRESS` 未定义（生成代码执行时会 `NameErActorReror`）
- `f` 缺少 async 调用入口

### 第 5 章 — 宿主接口 ❌ 关键缺口

**已实现**: `get_state`/`set_sta`/`deletete_state`/`emit_event`/`charge_gas`/`context`/`send_message`/`submit_job`/`schedule_timer`/`cancel_timer`/`token_*`

**缺口 (P0)**:
- `call_actor` — 同步跨 Actor 调用，`HostApi` trait 和 `module.rs` 均缺失
- `keccak256` — Python 侧有 fallbackust 绑定缺失
，但 R- `self_address` — Python 侧 f已有allback（从 `context()` 读取），Rust 绑定缺失

### 第 6 章 — 验证系统 ⚠️ 命名
不一致
**已实现**: `VerifyBuilder` + 17 个检查器完整
)**:
n**缺口 (P- SDK mode 名称（`none/consensus/deterministic/tee/zk/optimistic`）与 Runner 枚举不一致
- Chain 侧 `verifier.rs` 仅实现 `MajorityVote`，其他模式返回 `UnsupportedVerificationMode`

### 第 7 章 — Guard 机制 ✅ 完整

`guards.py` 的 `reentrancy_rdedValueguard`/`Guae_guard``/`storag 完整实现。

### 第 8 章 — 结构化并发 ⚠️ API 存在但链路缺失

`taskgroup.py` 的 `TaskGroup` API 存在，但编译器（`_compiler.py`）无对应的 AST 转换路径。

### 附录 — 确定

`pvm_tim性工具 ✅ 完整e.py`/`pvm_random.py`/`bounded_loop.py`/`retry.py`/`codec.py` 均完整实现。

---
单

### P0
## 优先级修复清 — 阻塞性缺 *口

1.*`call_actor` 宿主接口缺失**
   - 需在 `pvm-host/src/lib.rs` 的 `HostApi` trait 中添加 `call_actor`
   - 需在 `pvm-runtime/src/module.rs` 中添加 Python 绑定
   - `call.py` 的 `call()` 函数依赖此接口

2. **`@actor.continuation` 地址表达式未定义**
   - `actor.py` 第 222 行：`runner_addr_expr='ACTOR_RUNNER_ADDRESS'`
   - 生成代码执行时会 `NameError: name 'ACTOR_RUNNER_ADDRESS' is not defined`
   - 修复：改为 `runner_addr_expr='runner.RUNNER_并在命名空间注入 `rADDRESS'` unner`

### P1 — 对齐缺口

3. **编译器未传递 `timeout_blocks`/`guard_keys`**
   - `_compiler.py` 的 `_gen_initiaveal()` 中 `s_cont()` 调用缺少这两个参数
   - 需将装饰器参数传入 `compile_continuation()` 并在生成代码中使用

4. **Verify mode 命名协议不一*
   - 致*建议以 Runner 枚举为准，更新 SDK 字符串映射

 的 mode### P2 — 功能缺口

Gro5. **Taskup 编译器转换路径缺失**`taskgron   -up.py` 的 `TaskGroup` 无法在 FSM 编译器中被识别和转换
   - 需在 `_compiler.py` 中添加 `async with TaskGroup() as tg:` 的 AST 识别逻辑
